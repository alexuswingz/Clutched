rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Allow read/write access to all files for now
    // Since we're not using authentication, we'll use open access
    // In production, you might want to add more specific rules
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}
